[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-adk"
version = "0.1.0"
description = "Agent Development Kit for AgenticOS (local SDK + CLI)"
authors = [ {name = "AgenticOS"} ]

[project.scripts]
agentic = "adk.cli:main"
